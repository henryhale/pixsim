{
  "name": "pixsim",
  "version": "0.0.0",
  "description": "A pixel-based led matrix display simulator",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:apps": "tsc && vite build --outDir ./docs/public/app",
    "dev:docs": "pnpm build:apps && vitepress dev docs",
    "build": "pnpm build:apps && vitepress build docs",
    "preview": "vitepress preview docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "name": "Henry Hale",
    "url": "https://github.com/henryhale"
  },
  "license": "MIT",
  "devDependencies": {
    "marked": "^15.0.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.9",
    "vitepress": "^1.5.0"
  }
}
